User-agent: *
Allow: /

# The index, not sitemap.xml: it names this host's sitemap plus every
# subdomain's, so a crawler that starts here finds the whole network.
Sitemap: https://sadeali.com/sitemap-index.xml
